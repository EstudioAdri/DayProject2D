public static class Enums
{
    public enum Tags { player, interactable, ladder, platformPoint }
    public enum Direction { none, left, right }
    public enum PlayerState { Idle, Moving, Jumping, OnLadder }
    public enum PlatformState { Idle, PlayerOnIt, PlayerOffIt }
}
